// MVP or Model View Presenter Pattern its similar to MVC
// In MVC the View has access to both model and controllers but in MVP the View doesn't have to access the model, it has to get it from the Presenter and the Presenter acts as the logic and supply the data
