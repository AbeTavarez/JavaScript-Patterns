// MVC or Model View Controller Pattern help us organize our code in the following way:
// Model = Data (Define Schemas for the data)
// View = Visuals (HTML)
// Controllers = Logic (functions)
// (In MVC the View has access to both model and controllers)
